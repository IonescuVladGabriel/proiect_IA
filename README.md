# Reglarea parametrilor PID folosind Evoluția Diferențială (DE/rand/1/bin)
#
# Proiectul implementează varianta standard a algoritmului de evoluție diferențială
# DE/rand/1/bin și o aplică pentru reglarea automată a parametrilor unui regulator PID.
# Sistemul controlat este un sistem dinamic de ordinul doi, simulat numeric.

```bash
pip install -r requirements.txt
python3 -m src.main